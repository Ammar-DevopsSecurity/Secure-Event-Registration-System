# Secure-Event-Registration-System
Built a serverless 3-tier event registration system on AWS. Frontend hosted on S3, REST API via API Gateway + Lambda (Python), and MySQL database on RDS. Features real-time RSVP tracking, attendee stats, and a dynamic event dashboard. Configured CORS, IAM roles, and secure VPC networking.
